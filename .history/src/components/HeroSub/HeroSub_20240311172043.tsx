import "./style.css";

export const Heroub = (text: string): JSX.Element => {

  return (
    <>
      <header
        id={'start'}
        className={'hero'}
      >
        <p>{props.text}</p>
    </>
  );
}

