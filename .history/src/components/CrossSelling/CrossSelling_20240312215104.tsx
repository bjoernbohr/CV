import React from 'react';

export type HeadlineProps = {
  title?: string;
  showBorder?: boolean;
  customStyle?: string;
  delay?: number;
}

export const CrossSelling = (props: HeadlineProps): JSX.Element => {
  return (
    <section className={'cross-selling'}>
      
    </section>
  );
}

